# AutoProject 작업일지 — 2026-04-05 ~ 06

---

## 완성된 파이프라인 전체 흐름

```
Jira TC 조회
    ↓
Groq AI → TC 자동 생성 (generate_tc.py)
    ↓
Groq AI → Selenium pytest 코드 변환 (generate_selenium.py)
    ↓
pytest 실행 + 결과 JSON 저장 (run_tests.py)
    ↓
Jira 버그 티켓 자동 등록 (create_jira_bugs.py)
    ↓
Google Sheets 동기화 (sync_sheets.py)
    ↓
Slack 결과 알림 (notify_slack.py)
    ↓
HTML 대시보드 생성 (generate_dashboard.py)
```

실행: `python pipeline.py`

---

## 오늘 생성/수정된 파일

| 파일 | 설명 |
|------|------|
| `src/run_tests.py` | pytest 실행 + 결과 JSON 저장 (pytest-json-report) |
| `src/create_jira_bugs.py` | 실패 TC → Jira 버그 티켓 자동 등록 (중복 방지) |
| `src/notify_slack.py` | Slack Incoming Webhook 결과 알림 |
| `src/generate_dashboard.py` | Allure 스타일 HTML 대시보드 (SVG 도넛 차트) |
| `src/sync_sheets.py` | Google Sheets 연동 (TC+결과+버그 통합) |
| `src/generate_selenium.py` | Playwright → Selenium pytest 코드 자동 생성 |
| `src/create_jira_filters.py` | Jira JQL 필터 생성 스크립트 |
| `pipeline.py` | 전체 파이프라인 순차 실행 |
| `credentials.json` | Google Sheets 서비스 계정 키 (gitignore) |
| `requirements.txt` | selenium, webdriver-manager, pytest-json-report, gspread 추가 |

---

## Jira 설정

- **프로젝트**: MKQA (마켓컬리QA)
- **URL**: https://jmlim9244-1775142889491.atlassian.net
- **티켓 구성**
  - MKQA-1~7: TC 티켓 (기능 검증 5개)
  - MKQA-8~24: 자동 등록 버그 티켓 (Selenium 실패 결과)

### Jira 대시보드 가젯 구성
| 가젯 | 설정 |
|------|------|
| 파이 차트 | 이슈 유형 기준 (버그 77% / 작업 23%) |
| 필터 결과 | MKQA 버그 목록 필터 |
| 이슈 통계 | 이슈 유형 기준 |
| 생성 대비 해결 차트 | 최근 30일 버그 생성/해결 추이 |

---

## Google Sheets 설정

- **스프레드시트 ID**: `1yNIAlegsJCIT5mzWz6TyjgTncIW3_FAkEqWN6TOAM2Q`
- **서비스 계정**: `autoproject-qa@autoproject-492413.iam.gserviceaccount.com`
- **시트 구성**
  - `TC 실행 결과`: TC ID / Jira티켓(링크) / 기능명 / 테스트항목 / 사전조건 / 테스트단계 / 기대결과 / 실행결과 / 상태(PASS/FAIL 색상) / 버그티켓(링크)
  - `요약`: 전체/통과/실패/통과율/버그 수 요약

---

## Slack 연동

- **Webhook**: `T08CMQ2P45A/B0AQVR7H2Q6` (환경변수)
- **메시지 구성**: 상태 / 실행시각 / 전체·통과·실패 수 / 실패 항목 목록 / Jira 보드 링크
- **주의**: `attachments` 안에 `blocks` 사용 불가 → top-level `blocks` 사용

---

## Selenium 전환

- 기존 Playwright → **Selenium + pytest + webdriver-manager** 로 전환
- `generate_selenium.py`에서 Groq AI가 Selenium 코드 자동 생성
- TC 수는 프롬프트에서 조절 가능 (현재 3개 고정)

---

## QATEST Jira 연동 (진행 중)

- **파일 추가**: `utils/create_jira_bugs.py`
- **workflow 수정**: `Test_Automation.yaml`
  - `--json-report` 옵션 추가 (UI/API 각각)
  - `create_jira_bugs` job 추가 (ui_tests + api_tests 완료 후 실행)
  - Actions 버전 업데이트: `checkout@v3→v4`, `setup-python@v4→v5`
- **GitHub Secrets 추가 필요**:
  - `JIRA_URL`
  - `JIRA_EMAIL`
  - `JIRA_API_TOKEN`
  - `JIRA_PROJECT_KEY` = `QAT` (새 프로젝트)

---

## 남은 작업

- [ ] QATEST GitHub Secrets 등록 후 워크플로우 실행 확인
- [ ] AutoProject README 작성 (포트폴리오용)
- [ ] QATEST 전용 Jira 프로젝트 `QAT` 생성
- [ ] TC 수 3개 → 5개로 늘리기 (선택)

---

## 환경 변수 목록 (.env)

```
GROQ_API_KEY
JIRA_URL
JIRA_EMAIL
JIRA_API_TOKEN
JIRA_PROJECT_KEY
SLACK_WEBHOOK_URL
GOOGLE_CREDENTIALS_PATH
SPREADSHEET_ID
```
